# --- Snowflake & Data Stack ---
# Use >= to allow Airflow constraints to pick the best sub-version
snowflake-connector-python>=3.12.0
snowflake-sqlalchemy>=1.6.0
pandas>=2.1.0
pyarrow>=14.0.0
numpy>=1.26.0

# --- Orchestration ---
# This is our anchor version
apache-airflow==2.10.4

# --- Transformation (DBT) ---
# Aligned to 1.7+ which is compatible with the libraries above
dbt-core>=1.7.0
dbt-snowflake>=1.7.0

# --- Utils ---
# Use >= here so it doesn't fight the Airflow constraint for 2.6.1
confluent-kafka>=2.3.0
python-dotenv>=1.0.0
structlog>=24.1.0